"""Membership Editor page - Edit fuzzy membership functions."""

from __future__ import annotations

import streamlit as st


def render() -> None:
    """Render the Membership Editor page."""
    st.title("Membership Editor")
    st.info("Membership Editor — coming soon (Task 20)")
