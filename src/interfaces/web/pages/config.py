"""Configuration page - Edit system configuration."""

from __future__ import annotations

import streamlit as st


def render() -> None:
    """Render the Config page."""
    st.title("Config")
    st.info("Config — coming soon (Task 19)")
