"""Rules page - Create and manage natural language rules."""

from __future__ import annotations

import streamlit as st


def render() -> None:
    """Render the Rules page."""
    st.title("Rules")
    st.info("Rules — coming soon (Task 18)")
